/*
1. Создайте несколько переменных и поместите в них такие значения, как: ваше имя, фамилию, дату рождения, количество полных лет, а также уровень радости от 0 до 1. После этого выведите все переменные в консоль.
*/

let FirstName = "Aleksandr";
let LastName =  "Kuzhelev";
let Happy = 1;

let BirthYear = 1977;
let BirthMonth = 1; // Это февраль, индексация с 0, не понял сразу.
let BirthDay = 10;
let BirthDate = new Date(BirthYear,BirthMonth,BirthDay);
//для подсчета полных лет. С учетом дня рождения ДО или ПОСЛЕ текущей даты.
let Years = new Date().getFullYear() - BirthDate.getFullYear(); //разница в годах
let Age = Years;
if (new Date() - new Date(BirthYear + Years, BirthMonth, BirthDay) >=0)
  {
    Age = Years + 1;
  }
console.log("FirstName: ",FirstName,
            "\nLastName: ",LastName,
            "\nBirthDate = ",BirthDate.toLocaleDateString(),
            "\nAge =",Age,
            "\nHappy = ",Happy);

/*
2. Создайте переменную, которая будет принимать true или false.
- Также создайте переменную со значением -0.78902. Выведите эти переменные в консоль.
- Поменяйте значения в обоих переменных. Выведите эти новые переменные в консоль.
*/
let b = true;
let f = -0.78902;
console.log("b = ",b,"\nf = ",f);

b = false;
f = 0.12345
console.log("b = ",b,"\nf = ",f);

/*
3. Создайте две переменные со значениями 5.6 и 8.9.
- Выведите в консоль все математические операции с этими переменными (сумма, разница, умножение, деление и остаток при делении).
*/

let a = 5.6;
let b = 8.9;
console.log("a + b = ", a + b);
console.log("a - b = ", a - b);
console.log("b - a = ", b - a);
console.log("a * b = ", a * b);
console.log("a / b = ", a / b);
console.log("b / a = ", b / a);
console.log("a % b = ", a % b);
console.log("b % a = ", b % a);

/*
4. Создайте переменную со значением -2.
- Умножьте её на 3 и добавьте 1. Выведите результат в консоль.
- Отнимите 2 и поделите на 3. Выведите результат в консоль.
- Получите остаток от деления(%) на 2 и прибавьте 1. Выведите результат в консоль.
- Отнимите 1 и умножте на 4. Выведите результат в консоль.
- Прибавьте 1 и отнимите 2. Выведите результат в консоль.
Все это надо сделать при помощи сокращенных форм арифметических действий (-=, +=, /=, *=, %=, --, ++).
*/
let a = -2;
a *= 3;
a ++;
console.log(a);
a -= 2;
a /= 3;
console.log(a);
a %= 2;
a ++;
console.log(a);
a --;
a *= 4;
console.log(a);
a ++;
a -=2;
console.log(a);

/*
5. Создайте переменную "num" со значением 46 и переменную "car" со значением "string". Выведите результат в консоль.
- Умножьте числовую переменную на 5. Выведите в консоль обе переменные.
- Далее склейте обе переменные при выводе в консоль.
- Добавьте к результату склеивания двух переменных фразу "Result is ". Выведите результат в консоль.
- Поменяйте значения обоих переменный и выведите новый результат в консоль.
- Создайте еще одну переменную "num2" и присвойте ей значение 5.
- Создайте еще одну переменную "res" и присвойте ей значение как результат умножения "num" и "num2". Выведите все новые данные в консоль с фразой "My new result is: ".
*/

let num = 46;
let car = "string";
console.log(num, car);
num *=5;
console.log(num, car);
console.log(num+car);
console.log("Result is "+num+car);
num = 100;
car = "text";
console.log("Result is "+num+car);
let num2 = 5;
let res = num * num2;
console.log("My new result is: "+num2+res);


/*
6. Создайте 4 переменные со значениями: "5", "F", "Привет", "90.2".
- Создайте пятую переменную, которую нельзя будет изменить (const) и установите ей значение 67. Выведите переменную со значением "Привет" в консоль.
- Создайте шестую переменную и присвойте ей значения сложения "5" и "90.2" а так же склейки "F" и "Привет". Выведите значение этой новой переменной в консоль (должно получиться "95.2FПривет").
*/

let NumI = 5;
let Ch = "F";
let St = "Привет";
let NumF = 90.2;
const NumC = 67;

console.log(St);
let Mash = NumI+NumF+Ch+St;
console.log(Mash)

/*
 7. Создайте три переменных типа String и присвойте им три разных значения в виде фраз:
- простая фраза
- фраза в которой содержится другая фраза в одинарных кавычках ''
- фраза в которой содержится другая фраза в двойных кавычках ""
Выведите значения всех переменных в консоль.
*/

let st1 = "test";
let st2 = "outertest 'intest' outertest";
let st3 = 'outertest "intest" outertest';
console.log(st1);
console.log(st2);
console.log(st3);

/*
8. Создайте две числовых переменных и присвойте им значения больше миллиона.
- Запишите эти значения используя метод (e) или (e-).
- Выведите значения переменных в консоль добавя фразу "My huge awesome numbers is ".
- Создайте третью переменную с отрицательным значением больше миллиона и запишите ее через вышеописанный метод.
- Выведите в консоль результат умножения всех трех переменных с новой фразой "My magic looks like: ".
*/

let MegaPI = 3.14e7;
let MegaE = 2.72e7;
console.log("My huge awesome number is ", MegaPI);
console.log("My huge awesome number is ", MegaE);
let MegaMe = -9.1e7;
console.log("My huge awesome number is ", MegaMe);
console.log("My magic looks like: " + MegaPI*MegaE*MegaMe);

/*
9. Создайте две числовых переменных и присвойте им значения.
- Выведите значения переменных в консоль добавя фразу "My new numbers is ".
- Поменяйте значения одной из переменных на "null", выведите новые значения в консоль со старой фразой.
- Создайте новую переменную которая будет определять тип переменной (typeof()).
- Выведите в консоль значение этой новой переменной, которая должна определить тип ваших предыдущих переменных. При этом добавте фразу "My variables type is:".
- Измените вывод в консоль таким образом что бы было две фразы "My first variable is: " и далее должен определяться тип первой переменной, и вторая фраза "My second variable is: " и далее должен определяться тип второй переменной.
*/

let num = 10;
let kol = 20;
console.log("My new numbers is "+num+" "+kol);
num = null;
console.log("My new numbers is "+num+" "+kol);
let getType;
console.log("My variables type is:", typeof(getType));
getType = typeof(num);
console.log("My first variable is: "+getType);
getType = typeof(kol);
console.log("My second variable is: "+getType);

/*
10. Создайте переменную "myNumPi" и присвойте ей значение числа "Pi" с помощью метода "Math.PI".
- Выведите значения переменной в консоль добавя фразу "My PI number is: ".
- Создайте еще одну переменную "round" и присвойте ей значение как округление переменной "myNumPi" с помощью метода "Math.ceil".
- Выведите значения переменной "round" в консоль добавя фразу "My rounded PI number is: ".
- Измените значение переменной "round" чтобы она округляла переменную "myNumPi" в меньшую сторону при помощи метода "Math.floor".
- Выведите значения переменной "round" в консоль добавя фразу "My NEW rounded PI number is: ".
- Добавьте еще одну фразу при выводе в консоль к предыдущей фразе и результату округления, "But my original number PI is: " и к этой фразе приложите значение переменной "myNumPi".
*/

let myNumPi = Math.PI;
console.log("My PI number is:"+ myNumPi);
let round = Math.ceil(myNumPi);
console.log("My rounded PI number is: ", round)
round = Math.floor(myNumPi);
console.log("My new rounded PI number is: ", round);
console.log("But my original number PI is: "+myNumPi);

/*
11. Создайте три переменных типа number, string, boolean.
- Присвойте переменным соответствующие им значения.
- Переменная типа number должна быть отрицательная.
- Выведите значения всех трех переменных в консоль при этом поменяв в консоле отрицательное значение числовой переменной на положительное.
- Используйте для этого метод "Math.abs".
- Создайте еще одну числовую переменную с отрицательным значением.
- Создайте еще одну переменную и присвойте ей значение как результат разницы первой отрицательной переменной и второй.
- Выведите результат этой переменной в консоль поменяв ее значение на положительное при помощи метода "Math.abs".
- При выводе в консоль склейте значение этой новой переменной с переменными
типа string и boolean.
*/

let N1 = -5;
let St = "Text";
let Flag = true;
 console.log(Math.abs(N1), St, Flag);
 let N2 = -15;
 let Ndiff = N2 - N1;
 console.log(Math.abs(Ndiff)+St+Flag);

/*
12. Создайте две переменных типа string.
- присвойте им значения "txt1", "txt2".
- Выведите их значения в консоль добавив между ними пробел.
- Измените значение первой переменной добавив к ней фразу "это новая фраза" при помощи "txt1 +="
- Выведите измененные значения в консоль добавив перед ними фразу "Ух ты!", а так же пробел между ними.
*/

let st1 = "txt1";
let st2 = "txt2";
console.log(st1+" "+st2);
st1 += "это новая фраза";
console.log("Ух ты! "+st1+" "+st2);

















































